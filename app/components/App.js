import HomeSections from "./card/home";

// main component --------------------------------------------------

const Home = () => {
  return (
    <main className="relative w-full h-full">
      <HomeSections />
    </main>
  );
};

export default Home;
